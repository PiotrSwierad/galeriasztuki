<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<li>
			<%= gravatar_for user %>
			<%= link_to user.email, user %>
		</li>
	<% end %>
</ul>

<%= will_paginate %>
